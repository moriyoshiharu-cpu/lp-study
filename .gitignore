# .gitignore
# ここに書いたファイルは、Gitの管理対象から外れます（履歴に残りません）。
# OSやエディタが勝手に作る「中身に関係ないファイル」を除外するのが目的です。

# Windows が自動生成するファイル
Thumbs.db
desktop.ini

# macOS が自動生成するファイル
.DS_Store

# エディタの一時ファイル
*.swp
*~

# 各種エディタの設定フォルダ（人によって違うので共有しない）
.vscode/
.idea/
